- name: Install yq
  run: |
    mkdir -p "$HOME/bin"
    wget -O "$HOME/bin/yq" https://github.com/mikefarah/yq/releases/download/v4.48.2/yq_linux_amd64
    chmod +x "$HOME/bin/yq"
    echo "$HOME/bin" >> $GITHUB_PATH
    echo "yq version: $($HOME/bin/yq --version)"
